<?php

namespace App\Repositories{namespace};
{use}
class {class} extends Repository
{
    const CACHE_PREFIX = '{cache_prefix}';

    const MODEL_CN = '{model_name}';

    const MODEL = {model}::class;

    protected $fillable = [
        'id',
    ];

    /**
     * @param {model} $model
     * @return array
     */
    public function format($model): array
    {
        return parent::format($model); // TODO: Change the autogenerated stub
    }



}